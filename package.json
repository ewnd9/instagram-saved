{
  "name": "instagram-saved-posts-scraper",
  "version": "1.0.0",
  "description": "Scrape Instagram saved posts and store them in PostgreSQL",
  "main": "dist/index.js",
  "packageManager": "yarn@4.3.1",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "scrape": "npm run build && node dist/index.js",
    "scrape:details": "npm run build && node dist/index.js --details",
    "scrape:cli": "npm run build && HEADLESS=false node dist/cli.js",
    "scrape:cli:details": "npm run build && HEADLESS=false node dist/cli.js --details",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "keywords": [
    "instagram",
    "scraper",
    "postgresql",
    "playwright",
    "saved-posts"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@playwright/test": "1.55.0",
    "dotenv": "^17.2.1",
    "drizzle-kit": "^0.31.4",
    "drizzle-orm": "^0.44.5",
    "pg": "^8.16.3",
    "zod": "^4.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/pg": "^8",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
